# ST-YaKit-preinstall 开发说明

这是 YaKit 系列的 SillyTavern 提示词试验台仓库；当前仅有仓库基础文件，计划采用原生 JavaScript、HTML 和 CSS，无构建步骤。

## 仓库结构

```text
ST-YaKit-preinstall/
├── .gitignore       # 排除本地资料、测试、环境配置及缓存
├── README.md        # 用户说明与可用状态
├── Public.md        # 当前实现状态、接入方案与开发约定
└── AGENTS.md        # 本机共享规则的软链接，不入库
```

## 加载与数据流

当前没有扩展清单和运行入口，尚无加载、请求或评分流程。

首版接入方案：

1. 用户输入需求，可选提供原提示词；空白需求不进入生成流程。
2. 设计 AI 先生成测试场景与评分规则，用户可修改；开始比较后固定这一套方案。
3. 设计 AI 在独立请求中生成三个候选版本；有原提示词时将其作为基线，没有时使用不添加候选约束的同一写作背景作为基线。
4. 插件固定正文模型、写作背景、注入位置与采样配置，对每个候选版本分别组装消息并重复试写。
5. 裁判 AI 根据规则、事实依据和匿名正文给出判定及原文证据，不接收候选提示词文本、名称和历次排名。
6. 插件统计有效样本中的违规率和正常任务表现，保存正文及评分证据，提供比较和复制。
7. 后续接入真实预设回放，再加入有轮数和调用上限的自动优化；最终验收场景不提供给优化者。

边界处理方案：

| 情况 | 处理方式 |
| --- | --- |
| 当前没有聊天或角色 | 独立场景实验可用；真实聊天回放需要有效快照 |
| 测试方案或评分 JSON 缺字段、类型错误 | 标记本次结果无效，不作为成功或违规计分 |
| API 请求失败、取消或正文为空 | 记录执行状态，不混入有效样本统计 |
| 样本不完整、全部不合格或差异不明确 | 展示已完成证据，不强行推荐胜出者 |
| 正文模型知道某事实，但 NPC 没有信息来源 | 裁判依据角色的信息来源判断；合理猜测与声称确定知情分开处理 |
| NPC 后续被告知新信息 | 以该回合已发生的信息传递为准更新判定依据 |
| 用户修改场景、规则、背景或采样配置 | 作为新的实验条件，避免混合统计 |

## 设置与主题

当前没有已实现的设置字段。以下为首版拟提供的配置：

| 配置 | 含义或默认方案 |
| --- | --- |
| 设计连接 | 生成场景、评分规则和候选提示词；由用户选择 ST 连接配置 |
| 正文连接 | 用户实际用于角色扮演的模型连接，一轮实验内固定 |
| 裁判连接 | 判断正文表现，可与设计连接共用，调用上下文独立 |
| 候选数量 | 首版默认三个候选，基线单独保留 |
| 场景数量、重复次数 | 开始实验前确定，并显示预计正文调用次数；设计和评分调用另计 |
| 输出长度、采样参数 | 同一轮候选之间保持一致，按目标模型支持情况使用 |

三个角色允许使用同一连接配置；隔离通过独立消息和请求状态实现。正文请求不继承设计讨论、评分规则和历史报告。候选提示词本身表达的目标仍然会被正文模型看到。

界面尚未实现。后续主面板沿用 ST 主题，其他界面的尺寸和微动效与主面板保持一致。

## 公开 API

当前没有向其他插件公开的 API，也没有可调用示例。

## 当前接入状态

| 项目 | 状态与边界 |
| --- | --- |
| 仓库基础文件 | 已建立忽略规则、用户说明和开发说明 |
| ST 扩展加载、界面 | 尚未接入 |
| 模型连接 | 拟复用 `SillyTavern.getContext().ConnectionManagerRequestService.sendRequest()`；本地宿主源码存在该接口，尚未进行请求验收 |
| 提示词模板 | 尚未实现；工作指令由开发者维护，候选约束由设计 AI 根据用户需求生成 |
| 裁判与统计 | 尚未实现；AI 负责语义判断，代码负责字段检查、有效样本统计和排名，用户可查看证据复核 |
| 记录保存与导出 | 尚未接入 |
| 真实预设回放 | 尚未接入；需要保留宿主组装后的消息顺序并明确替换被测条目 |
| 自动迭代、预设写回 | 尚未接入 |
| 宿主依赖与最低版本 | 运行时代码尚无依赖；接入后按实际接口与验收结果确定最低版本 |

模型接入时由插件显式构造各角色的消息数组，通过 ST 连接配置调用。连接配置中的生成参数不等于完整的角色卡、世界书和预设消息组装结果，两者分别处理。

`generateQuietPrompt()` 会包含聊天上下文；本地 `generateRaw()` 实现仍会触发允许扩展修改消息的事件。隔离验收应检查实际发出的消息，不能只依据接口名称。

工作指令职责：

| 指令 | 内容 |
| --- | --- |
| 设计指令 | 将需求转成可观察行为，设计失败与正常使用场景，给出可引用事实依据的评分规则，再生成不同约束策略的候选提示词 |
| 正文指令 | 固定的普通续写要求；候选约束在统一位置插入，其他试验规则不额外注入 |
| 裁判指令 | 仅按已固定的规则评价匿名正文，每次违规给出原文证据与事实依据，证据不足时说明无法判定 |

## 开发与验证

- 当前生效规则通过本机 `AGENTS.md` 入口读取；该入口指向仓库外的共享规则，不复制规则正文。
- UI 修改和业务逻辑修改分开执行；按实际职责拆分模块，新代码使用通俗的中文注释。
- 用户可感知的功能变化同步更新 README，模块、数据流、接口与验证方式变化同步更新本文。
- 需求、配色、设计、测试和环境资料按 `.gitignore` 保留在本地。
- 每次完成已授权的工作后提交，提交信息使用中文说明工作及完成程度。

当前没有测试脚本或构建命令。仓库基础检查使用 `git diff --check`；忽略规则使用 `git check-ignore --no-index --stdin` 检查代表性路径。

实现后的 ST 验收按本机 `AGENTS.md` 中的“ST开发验收流程”执行：使用内置浏览器连接本地酒馆，通过实际交互验证；直接连接仍不可用时转人工验收。

宿主接口参考：[ST 扩展开发说明](https://docs.sillytavern.app/for-contributors/writing-extensions/)。
