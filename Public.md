# ST-YaKit-preinstall 开发说明

这是 YaKit 系列的 SillyTavern 提示词工作台仓库；当前仅有仓库基础文件，计划采用原生 JavaScript、HTML 和 CSS，无构建步骤。

## 仓库结构

```text
ST-YaKit-preinstall/
├── .gitignore       # 排除本地资料、测试、环境配置及缓存
├── README.md        # 用户说明与可用状态
├── Public.md        # 当前实现状态、接入方案与开发约定
└── AGENTS.md        # 本机共享规则的软链接，不入库
```

## 加载与数据流

当前没有扩展清单和运行入口，尚无加载、请求或反馈流程。

首版接入方案：

1. 用户在独立工作台中输入需求，可选提供原提示词；空白需求不进入生成流程。
2. 工作台 AI 设计提示词，按需提供多个方案或试写场景；用户通过对话或手动编辑修改草稿。
3. 用户选定要试写的版本。插件保存该版本的内容，并记录本次正文连接、使用位置与写作背景。
4. 将选定版本用于指定位置，使用用户平时的正文连接与 ST 正常写作上下文生成正文。工作台讨论单独保存，不混入正文请求。
5. 用户阅读正文，判断是否达到预期，可引用问题片段、说明原因和期望表现；反馈关联本次正文及其提示词版本。
6. 用户发起继续修改时，工作台 AI 根据需求、当前版本、用户选定的正文或片段及反馈提出下一版提示词；旧版与已有反馈保留。
7. 用户满意后复制选中的提示词。预设保存入口将在实际接入后确定。

职责分配：

| 参与者 | 职责与可见信息 |
| --- | --- |
| 工作台 AI | 读取需求、草稿、修改讨论和用户提交的试写反馈，负责设计与修改提示词 |
| 正文 AI | 读取选定提示词及该次写作需要的角色、场景、聊天等正常上下文，负责生成正文 |
| 用户 | 阅读正文，判断是否达到预期，决定如何修改、是否继续试写及采用哪个版本 |
| 插件代码 | 管理对话、草稿版本、模型调用及正文与反馈的关联 |

边界处理方案：

| 情况 | 处理方式 |
| --- | --- |
| 当前没有聊天或角色 | 工作台可继续设计提示词；通过真实聊天试写时需要有效的写作上下文 |
| 模型请求失败、取消或结果为空 | 保留已有草稿和记录，显示本次状态，由用户决定是否重试 |
| 用户尚未评价正文 | 保持待反馈状态，不推断用户是否满意 |
| 用户选定版本后继续修改草稿 | 已有正文仍关联试写时保存的版本，新草稿单独保存 |
| 用户更换正文模型、使用位置或写作背景 | 新试写记录保存本次条件，供人工比较时参考 |
| 用户只选中部分正文反馈 | 工作台接收所选片段与用户说明，保留其与原正文的关联 |

## 设置与主题

当前没有已实现的设置字段。以下为首版拟提供的配置：

| 配置 | 含义或默认方案 |
| --- | --- |
| 工作台连接 | 用户选择的 ST 连接配置，用于提示词设计与修改 |
| 正文连接 | 使用用户平时的正文模型连接，每次试写记录实际使用的配置 |
| 提示词使用位置 | 用户明确选定的条目或插入位置，接入时按实际预设结构处理 |
| 正文输出长度、采样参数 | 沿用日常正文配置，每次试写记录实际条件 |

工作台和正文允许使用同一模型连接，但对话上下文分别管理。工作台可保留连续修改讨论；正文仅接收选定提示词与正常写作上下文。候选提示词本身表达的目标仍然会被正文模型看到。

界面尚未实现。后续主面板沿用 ST 主题，其他界面的尺寸和微动效与主面板保持一致。

## 公开 API

当前没有向其他插件公开的 API，也没有可调用示例。

## 当前接入状态

| 项目 | 状态与边界 |
| --- | --- |
| 仓库基础文件 | 已建立忽略规则、用户说明和开发说明 |
| ST 扩展加载、界面 | 尚未接入 |
| 工作台模型连接 | 拟复用 `SillyTavern.getContext().ConnectionManagerRequestService.sendRequest()`；本地宿主源码存在该接口，尚未进行请求验收 |
| 提示词设计与编辑 | 尚未实现；工作台指令由开发者维护，提示词内容由 AI 建议和用户编辑形成 |
| 正文试写 | 尚未接入；使用日常正文连接和 ST 正常提示词组装，具体使用位置及结果关联待实现 |
| 人工反馈与修改 | 尚未实现；用户判断效果并提交意见，工作台 AI 在用户发起修改后生成下一版 |
| 版本、正文与反馈记录 | 尚未接入 |
| 复制、导出与预设保存 | 尚未接入 |
| 宿主依赖与最低版本 | 运行时代码尚无依赖；接入后按实际接口与验收结果确定最低版本 |

工作台由插件显式构造消息数组，通过 ST 连接配置调用。正文沿用正常写作上下文，接入时需明确选定提示词的位置，并保留角色卡、世界书和其他预设条目的组装顺序。连接配置中的生成参数不等于完整的正文消息组装结果。

`generateQuietPrompt()` 会包含聊天上下文；本地 `generateRaw()` 实现仍会触发允许扩展修改消息的事件。接入验收应分别检查工作台与正文实际发出的消息，确认内容符合各自职责。

工作指令职责：

| 指令 | 内容 |
| --- | --- |
| 工作台指令 | 理解用户想要的效果，生成可编辑提示词；根据用户反馈与正文片段修改，简要说明每次修改针对的问题 |
| 正文输入 | 选定提示词与本次正常写作上下文，使用用户日常模型生成正文 |

## 开发与验证

- 当前生效规则通过本机 `AGENTS.md` 入口读取；该入口指向仓库外的共享规则，不复制规则正文。
- UI 修改和业务逻辑修改分开执行；按实际职责拆分模块，新代码使用通俗的中文注释。
- 用户可感知的功能变化同步更新 README，模块、数据流、接口与验证方式变化同步更新本文。
- 需求、配色、设计、测试和环境资料按 `.gitignore` 保留在本地。
- 每次完成已授权的工作后提交，提交信息使用中文说明工作及完成程度。

当前没有测试脚本或构建命令。仓库基础检查使用 `git diff --check`；忽略规则使用 `git check-ignore --no-index --stdin` 检查代表性路径。

实现后的 ST 验收按本机 `AGENTS.md` 中的“ST开发验收流程”执行：使用内置浏览器连接本地酒馆，通过实际交互验证；直接连接仍不可用时转人工验收。

宿主接口参考：[ST 扩展开发说明](https://docs.sillytavern.app/for-contributors/writing-extensions/)。
