# ST-YaKit-preinstall

YaKit 系列的 SillyTavern 提示词工作台，帮助你把写作需求变成提示词，通过正文试写和人工反馈逐版改进。

## 功能特性

- 从扩展菜单的「工作台」进入，桌面与显示器图标便于辨认。
- 与 AI 讨论需求，生成提示词，也能直接编辑草稿。
- 保存多个独立版本，每次试写和反馈都对应当时使用的版本。
- 使用当前聊天和主 API 试写正文，引用问题片段并交回修改。
- 为提示词设计选择主 API 或副 API，副 API 可使用已有连接配置或自定义接口。
- 通过常驻的顶部导航切换工作台、试写与反馈、版本记录和设置，四页随浏览器窗口一起调整大小，并可分别滚动。
- 与 YaKit 纪实采用相同的林系风、浅色、深色和跟随酒馆主题，统一按钮、下拉选项、滚动条与切页动效。
- 自动保存需求、草稿和已提交的记录，支持复制提示词、导出工作记录。

## 安装方法

装好后可以在当前聊天中完成「生成提示词 → 保存版本 → 试写 → 反馈修改」；首次使用请先连接 SillyTavern 的主 API。

1. 将完整项目文件夹放入 SillyTavern 的 `public/scripts/extensions/third-party/` 目录，保留文件夹内的目录结构。
2. 刷新 SillyTavern，打开角色或群组聊天，从扩展菜单点击桌面与显示器图标旁的「工作台」。
3. 默认使用主 API；若要单独配置设计用模型，在「设置」中选择副 API、填写连接信息并点击「保存设置」。
4. 回到「工作台」，填写需求并点击「生成提示词」，将草稿「保存为版本」。
5. 进入「试写与反馈」，填写场景并点击「试写选中版本」，阅读正文后评价、填写意见，再点击「按反馈修改」。

## 使用说明

1. **描述需求。** 从扩展菜单打开「工作台」，在「你希望改善什么？」中填写目标，在下方继续补充要求，点击「生成提示词」。修改讨论展示 AI 的说明，可展开「查看提示词」阅读完整建议；正常生成的提示词会进入草稿区。
2. **编辑并保存。** 在「工作台」直接修改草稿，填写可选的版本名称并点击「保存为版本」。每次保存新增一版；在「版本记录」选择旧版本可查看原文，并将它载入草稿。
3. **试写并查看。** 进入「试写与反馈」，填写要检验的场景并点击「试写选中版本」。正文使用当前角色或群组聊天的背景和主 API。草稿改过后需要先回「工作台」保存新版本。
4. **提交反馈。** 选择「达到预期」或「还需修改」，填写具体意见。需要引用时选中正文并点击「引用选中正文」，或粘贴该正文里的片段。点击「保存反馈」可以先保留评价。
5. **按反馈修改。** 点击「按反馈修改」会保存当前反馈，再围绕这次试写对应的版本生成下一版。有引用时交回引用片段；未引用时交回该次完整正文。完成后点击「返回编辑」查看草稿，保存新版本后可以继续试写。
6. **保存成果。** 工作台中的「复制提示词」复制当前草稿；版本记录中的「导出工作记录」下载需求、草稿、讨论、版本、正文和反馈。记录自动随酒馆设置保存；导出的文件不包含副 API 密钥。
7. **切换主题。** 在「设置」中选择林系风、浅色、深色或跟随酒馆，窗口、顶部按钮、下拉选项和滚动条立即同步配色。
8. **切页与退出。** 点击常驻的顶部页签切换页面，工作台会随浏览器窗口自动调整大小，四周保留少量留白；点击右上角关闭按钮、窗口外的遮罩或按 Esc 返回酒馆。切换页面或关闭后重新打开，会保留本次尚未提交的输入。

生成过程中可点击「取消」，已有内容会保留。主 API 请求可能仍需等待模型结束后才能再次发起；生成期间手动编辑的草稿不会被返回的建议覆盖。

## 配置说明

| 设置 | 用法 |
| --- | --- |
| 界面主题 | 默认跟随酒馆，可切换林系风、浅色或深色；选择后立即保存 |
| 工作台 AI | 负责提示词设计与修改，默认主 API，也可选择副 API |
| 已保存的连接配置 | 使用 SillyTavern 连接管理中的可用配置；列表为空时先在酒馆保存连接配置 |
| 自定义 OpenAI 兼容接口 | 填写完整接口地址和模型名；需要认证的服务还需填写 API 密钥 |
| 正文 AI | 始终使用当前主 API，在设置中显示当前类型 |

API 选择和连接信息填写后点击「保存设置」。选择了副 API 但连接配置缺失时会提示补全。密钥随酒馆设置保存；导出工作记录不包含密钥。

## 兼容性

最低支持 **SillyTavern 1.18.0**：本插件依赖该版本确认提供的独立生成、静默生成和连接请求接口。请通过酒馆扩展菜单打开工作台。

支持自定义下拉样式的浏览器会显示统一配色、可换行的选项弹层；其他浏览器使用系统下拉菜单，外观由浏览器和系统决定。

当前版本已完成核心和适配器的本地检查；主 API、副 API 的真实模型调用及界面交互仍待人工验收。

## 常见问题（FAQ）

**为什么试写按钮不可用？**

先打开角色或群组聊天、连接主 API，再保存提示词版本；草稿与已保存版本不一致时需要另存新版本。

**试写会直接发到聊天里吗？**

试写正文显示在工作台，不会自动作为聊天消息发送。

**为什么点击取消后仍提示主 API 忙碌？**

取消后工作台会忽略迟到的结果；酒馆的主 API 请求可能还在运行，等它结束后再试。

**保存失败怎么办？**

先点击「导出工作记录」留存当前内容，确认下载完成后再关闭。未提交的反馈和设置仍需先保存。

**能导入记录或直接写回预设吗？**

当前支持导出留存和复制提示词，尚未提供导入或自动写回预设。

## 更新日志

<details>
<summary>v0.2.6 · 自适应工作台</summary>

### v0.2.6

- 顶部导航保持常驻，移除隐藏导航按钮。
- 工作台随浏览器窗口放大，四页共用自适应尺寸，四周保留少量留白。

</details>

<details>
<summary>v0.2.5 · 主题与界面同步</summary>

### v0.2.5

- 与 YaKit 纪实统一四种主题、窗口大小、卡片和控件样式。
- 四页使用顶部导航和相同的滑动切页动效，各页保留输入与滚动位置。
- 打开、关闭窗口使用一致的微动效，并跟随系统的减少动态效果设置。
- 顶部保留导航开关与关闭按钮，当前页面名称仅供读屏。

</details>

<details>
<summary>v0.2.4 · 工作台入口</summary>

### v0.2.4

- 扩展菜单入口更名为「工作台」，采用桌面与显示器图标，并随菜单文字配色。

</details>

<details>
<summary>v0.2.3 · 顶部控件与下拉适配</summary>

### v0.2.3

- 导航开关移到工作台上方，顶部右侧的关闭按钮随主题配色，滚动页面时仍可使用。
- 设置、版本和试写记录的下拉选项统一配色，支持长名称换行和窄屏限宽。
- 保留关闭后重新打开的输入，并统一四页顶部控件的尺寸和微动效。

</details>

<details>
<summary>v0.2.2 · 滚动条适配</summary>

### v0.2.2

- 页面、讨论区和文本框统一使用随主题配色的细滚动条与透明轨道。

</details>

<details>
<summary>v0.2.1 · 分区与导航</summary>

### v0.2.1

- 工作台保留需求讨论与提示词编辑，试写反馈、版本记录分设独立页面。
- 侧栏支持收起与展开，各页面共用留白、主题和操作样式。
- 版本记录支持查看保存时间、完整提示词和导出工作记录。

</details>

<details>
<summary>v0.2.0 · SillyTavern 接入</summary>

### v0.2.0

- 从酒馆扩展菜单打开工作台，接入真实主 API 和副 API。
- 使用当前聊天试写正文，保存版本并按人工反馈继续修改。
- 增加工作台与设置侧栏、跟随酒馆及浅色和深色主题。
- 工作记录随酒馆设置保存，导出时排除副 API 密钥。

</details>

<details>
<summary>v0.1.0 · 交互预览</summary>

### v0.1.0

- 提供需求、草稿、版本、试写和反馈的独立交互流程。
- 支持手动编辑、复制、工作记录导出和取消操作。

</details>
